CREATE DATABASE IF NOT EXISTS espiaso;

USE espiaso;

DROP TABLE IF EXISTS DiscCourse;
DROP TABLE IF EXISTS Relation;
DROP TABLE IF EXISTS Discipline;
DROP TABLE IF EXISTS Course;

CREATE TABLE IF NOT EXISTS Discipline (
	Code CHAR(255) NOT NULL,
    Name CHAR(255) NOT NULL,
    Slug CHAR(255) NOT NULL,
    HasContent BOOL DEFAULT False,
    PRIMARY KEY (Code)
);

CREATE TABLE IF NOT EXISTS Course (
	ID INT UNSIGNED NOT NULL,
    Name CHAR(255) NOT NULL,
    Slug CHAR(255) NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE IF NOT EXISTS DiscCourse (
    ID INT UNSIGNED NOT NULL AUTO_INCREMENT,
	Code CHAR(255) NOT NULL,
    CourseID INT UNSIGNED NOT NULL,
    Semester SMALLINT NOT NULL,
    PRIMARY KEY (ID),
    FOREIGN KEY (CourseID) REFERENCES Course(ID)
);

CREATE TABLE IF NOT EXISTS Relation (
    Code CHAR(255) NOT NULL,
    Dependency CHAR(255) NOT NULL,
    FOREIGN KEY (Code) REFERENCES Discipline(Code)
);

SELECT * FROM Discipline;
SELECT * FROM Course;
SELECT * FROM Relation;
SELECT * FROM DiscCourse WHERE ID = 1;
SELECT * FROM DiscCourse;

DROP DATABASE espiaso;

ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'mJjlmnu3uO/o';
flush privileges;